"""
This module asks for a number n and then prints out the n:th Fibonacci number.
"""

def fib(n):
    """
    Returns the n:th Fibonacci number.
    """
    if n == 1 or n == 2:
        return 1
    return fib(n -1 ) + fib(n - 2)

def main():
    """
    Asks for a number n and prints out the n:th Fibonacci number.
    """
    n = int(input("Which Fibonacci number do you want to know? "))
    print(f'F({n}) = {fib(n)}')

main()
